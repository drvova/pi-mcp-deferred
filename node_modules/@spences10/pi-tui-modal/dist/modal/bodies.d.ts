import { type Focusable, type SettingItem, type SettingsListTheme } from '@earendil-works/pi-tui';
import type { InputModalOptions, ModalBody, ModalText, ModalTheme } from './types.js';
export declare class TextModalBody implements ModalBody {
    private readonly text;
    private max_visible_lines;
    private readonly theme;
    private readonly on_cancel;
    private offset;
    private wrapped_lines;
    constructor(text: ModalText, max_visible_lines: number, theme: ModalTheme, on_cancel: () => void);
    set_max_visible_lines(max_visible_lines: number): void;
    render(width: number): string[];
    invalidate(): void;
    handleInput(data: string): void;
}
export declare class InputModalBody implements ModalBody, Focusable {
    private readonly options;
    private readonly theme;
    private readonly on_submit;
    private readonly on_cancel;
    private readonly input;
    private _focused;
    get focused(): boolean;
    set focused(value: boolean);
    constructor(options: InputModalOptions, theme: ModalTheme, on_submit: (value: string) => void, on_cancel: () => void);
    render(width: number): string[];
    invalidate(): void;
    handleInput(data: string): void;
}
export declare class DetailedSettingsList implements ModalBody {
    private readonly items;
    private max_visible;
    private readonly theme;
    private readonly on_change;
    private readonly on_cancel;
    private readonly options;
    private filtered_items;
    private selected_index;
    private search_input?;
    constructor(items: SettingItem[], max_visible: number, theme: SettingsListTheme, on_change: (id: string, new_value: string) => void, on_cancel: () => void, options: {
        enable_search?: boolean;
        detail?: (item: SettingItem) => string | undefined;
    });
    get_selected_item(): SettingItem | undefined;
    set_max_visible(max_visible: number): void;
    render(width: number): string[];
    invalidate(): void;
    handleInput(data: string): void;
    private get_display_items;
    private render_item;
    private activate_item;
    private apply_filter;
    private add_hint_line;
}

type EvalCategory = 'search' | 'retrieval' | 'lifecycle' | 'capture' | 'retention' | 'cost' | 'dedupe';
interface EvalCaseResult {
    name: string;
    category: EvalCategory;
    passed: boolean;
    description: string;
    detail: string;
    returned_bytes: number;
    result_count: number;
}
interface EvalReport {
    version: 1;
    summary: {
        passed: number;
        failed: number;
        total: number;
        score_pct: number;
        returned_bytes: number;
    };
    sections: Array<{
        category: EvalCategory;
        passed: number;
        failed: number;
        total: number;
        score_pct: number;
    }>;
    results: EvalCaseResult[];
}
export declare function run_context_eval(): EvalReport;
export declare function run_context_eval_cli(args?: string[]): Promise<void>;
export {};
